services:
  - type: web
    name: flower-hub-todo
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt && python manage.py migrate && python manage.py collectstatic --no-input"
    startCommand: gunicorn todoproject.wsgi --bind 0.0.0.0:$PORT --log-file -
